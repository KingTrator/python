from math import sqrt
n = int(input('Digite um número inteiro para descobrir sua raiz quadrada aproximada em duas casas decimais'))
r = sqrt(n)
print('A raiz quadrada de {} é {:.2f}'.format(n, r))

#O código está funcional.